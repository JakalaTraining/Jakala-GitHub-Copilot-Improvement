// Scrivi un'API REST in PHP che gestisce utenti con:
// - Un endpoint per creare un utente (POST)
// - Un endpoint per ottenere un utente per ID (GET)
// - Un endpoint per eliminare un utente (DELETE)

// Confronto tra modelli:

// Modello: GPT-4
// Genera un'API REST completa con gestione degli errori e sicurezza (PDO, JSON).
// Perfetto per codice di produzione.

// Modello: Claude
// Genera un'API più semplice, senza validazione approfondita.
// Utile per prototipi.

// Modello: o1
// Potrebbe generare codice parziale o con errori.
// Non consigliato per API complesse.