/* Prompt:
   Scrivi un'API REST in PHP per gestire utenti.
*/

/* Problema:
   - Non specifica quali endpoint servono (GET, POST, DELETE, ecc.).
   - Non indica se deve usare un database o rimanere stateless.
   - Non menziona se la risposta deve essere in formato JSON.
*/

/* Soluzione:
   - Elencare esplicitamente gli endpoint richiesti e i loro parametri.
   - Chiarire se l'API deve interagire con un database (es. MySQL).
   - Definire il formato della risposta (JSON con status code appropriati).
*/
